import 'package:flutter/material.dart';

class AdmingWindowsScreen extends StatelessWidget {
  const AdmingWindowsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Admin Windows"),);
  }
}